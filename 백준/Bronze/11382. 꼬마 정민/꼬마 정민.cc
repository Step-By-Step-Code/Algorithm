#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	long long input1, input2, input3;
	cin >> input1;
	cin >> input2; 
	cin >> input3;
	cout << input1 + input2 + input3 << "\n";

	return 0;
}