#include <iostream>

using namespace std;

int main() {
    int A, B, C, D, E;
    cin >> A >> B >> C >> D >> E;

    int result = A + B + C + D + E;
    cout << result << "\n";
    return 0;
}